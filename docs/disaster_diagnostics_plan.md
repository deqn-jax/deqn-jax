# Disaster Scalar Diagnostics Plan

Generated by Ralph Loop. Date: 2026-04-25.

## Already done
- eq2b — `_eq2_diagnostics` (K_p recursion)
- eq4b — `_eq4_diagnostics` (K_w recursion)

## Summary
- DECOMPOSE: 5, LIGHT: 3, SKIP: 1
- Stop: every item DONE/SKIP/BLOCKED.
- Counts: TODO=3, DONE=6, SKIP=0, BLOCKED=0

## Workplan (ordered)

### 1. eq1 — price_phillips_F  [DECOMPOSE]  [STATUS: DONE]
**Residual:** `(lambda_z * y_z + beta*xi_p * (pi_tilda_n/pi_n)^(1/(1-lambda_f)) * F_p_n) / F_p - 1`
**Decompose into:**
  - `lhs_term` = `lambda_z * y_z` (now-side)
  - `expect_term` = `beta * xi_p * eq1_expect` (next-side)
  - `ratio_base` = `pi_tilda_n / pi_n` — mean/std/min/max
  - `eq1_ratio` = `ratio_base ** (1/(1-lambda_f))` — mean
  - `F_p`, `F_p_n` — means
  - `log_residual` = `log(rhs) - log(F_p)` — mean/std
  - `exponent` = constant `1/(1-lambda_f)`
**New keys:** `eq1_diag/{ratio_base_*, eq1_ratio_mean, F_p_mean, F_p_n_mean, lhs_term_mean, expect_term_mean, log_residual_mean, log_residual_std, exponent}`
**Cost:** S (mirrors eq2 template)

### 2. eq3 — wage_phillips_F  [DECOMPOSE]  [STATUS: DONE]
**Residual:** `(h*(1-tau_l)/lambda_w * lambda_z + beta*xi_w * eq3_expect) / F_w - 1`, where eq3_expect has **multiple multiplicative ratios**: `mu_z^(...)`, `pi_w_tilda^(...)`, `(1/pi_w)^(...)`, `1/pi_n`.
**Decompose into:**
  - `lhs_term` = `h*(1-tau_l)/lambda_w * lambda_z`
  - `expect_term` = `beta * xi_w * eq3_expect`
  - `mu_z_factor` = `mu_z_n^(iota_mu/(1-lambda_w) - 1) * mu_z_ss^((1-iota_mu)/(1-lambda_w))` — mean
  - `pi_w_tilda_factor` = `pi_w_tilda_n^(1/(1-lambda_w))` — mean
  - `pi_w_inv_factor` = `(1/pi_w_n)^(lambda_w/(1-lambda_w))` — mean
  - `pi_inv_factor` = `1/pi_n` — mean
  - `F_w`, `F_w_n` — means
  - `log_residual` mean/std
  - exponents on mu_z, pi_w_tilda, pi_w_inv (constants)
**New keys:** `eq3_diag/{lhs_term_mean, expect_term_mean, mu_z_factor_mean, pi_w_tilda_factor_mean, pi_w_inv_factor_mean, pi_inv_factor_mean, F_w_mean, F_w_n_mean, log_residual_mean, log_residual_std, exponent_pi_w_tilda, exponent_pi_w_inv}`
**Cost:** M (more multiplicative factors than eq1)

### 3. eq2a — Kp_definition  [LIGHT]  [STATUS: DONE]
**Residual:** `log(K_p) - log(F_p * K_p_inner^(1-lambda_f))` (algebraic identity, no expectation)
**Decompose into:**
  - `K_p_analytical` = `F_p * K_p_inner^(1-lambda_f)` — mean
  - `K_p` — mean (network output)
  - `log_residual` mean/std (same as the residual itself, but computed fresh)
  - `K_p_inner_floor_frac` — fraction at soft-floor (≤ 0.02). Distinct from eq2b's because eq2b uses next-period defs in some places.
**New keys:** `eq2a_diag/{K_p_mean, K_p_analytical_mean, log_residual_mean, log_residual_std, K_p_inner_floor_frac}`
**Cost:** S (light; no rollout step needed — uses current `defs` only)

### 4. eq4a — Kw_definition  [LIGHT]  [STATUS: DONE]
**Residual:** `log(K_w) - log((1/psi_L) * K_w_inner^(1-lambda_w*(1+sigma_L)) * w_tilda * F_w)` (algebraic identity)
**Decompose into:**
  - `K_w_analytical` = `(1/psi_L) * K_w_inner^(1-lambda_w*(1+sigma_L)) * w_tilda * F_w` — mean
  - `K_w` — mean
  - `log_residual` mean/std
  - `K_w_inner_floor_frac` — fraction at soft-floor
**New keys:** `eq4a_diag/{K_w_mean, K_w_analytical_mean, log_residual_mean, log_residual_std, K_w_inner_floor_frac}`
**Cost:** S (mirrors eq2a)

### 5. eq5 — consumption_euler  [DECOMPOSE]  [STATUS: DONE]
**Residual:** `((1+tau_c)*lambda_z*habit_now + beta*b*habit_now/habit_next) / mu_z - 1`, where `habit_now`, `habit_next` are each `_soft_floor(...)` at 1e-2.
**Decompose into:**
  - `tax_term` = `(1+tau_c) * lambda_z * habit_now` — mean
  - `habit_ratio_term` = `beta * b * habit_now / habit_next` — mean
  - `habit_now`, `habit_next` — means
  - `habit_now_floor_frac`, `habit_next_floor_frac` — saturation at 1e-2
  - `habit_now_over_next` — mean/std/min/max (this is the ratio that drives the second term)
  - `log_residual` mean/std
**New keys:** `eq5_diag/{tax_term_mean, habit_ratio_term_mean, habit_now_mean, habit_next_mean, habit_now_floor_frac, habit_next_floor_frac, habit_ratio_*, log_residual_mean, log_residual_std}`
**Cost:** M (needs zero-shock rollout for habit_next)

### 6. eq7 — investment_euler  [DECOMPOSE]  [STATUS: DONE]
**Residual:** `(mu_ups*q*(1 - S_val - i_ratio*S_prime_val) + beta*mu_ups*expect/lambda_z) - 1`, with `expect = lambda_z_n*q_n*mu_z_n*(i_n/i)^2*S_prime_next`.
**Decompose into:**
  - `now_term` = `mu_ups * q * (1 - S_val - i_ratio*S_prime_val)` — mean
  - `expect_term` = `beta * mu_ups * expect / lambda_z` — mean
  - `i_ratio_now` = `mu_z * i / i_lag` — mean (already in defs as `i_ratio`)
  - `i_ratio_next` = `mu_z_n * i_n / i` — mean/std/min/max (squared in expect, big nonlinearity vector)
  - `i_ratio_next_sq` — mean (the actual factor used)
  - `S_val`, `S_prime_val`, `S_prime_next` — means
  - `q`, `q_n` — means
  - `log_residual` mean/std (residual is `RHS - 1`, so log_residual = log(RHS+1) maybe; or just log of |RHS|)
**New keys:** `eq7_diag/{now_term_mean, expect_term_mean, i_ratio_next_*, i_ratio_next_sq_mean, S_val_mean, S_prime_val_mean, S_prime_next_mean, q_mean, q_n_mean, log_residual_mean, log_residual_std}`
**Cost:** M (zero-shock rollout, several derived quantities)

### 7. eq8 — entrepreneur_contract  [DECOMPOSE]  [STATUS: TODO]
**Residual:** `Rk_over_R * (1 - Gamma_next) - ratio_term * bracket_term`, where `Gamma`, `G` are computed at `omega_bar_next` (Newton-solved).
**Decompose into:**
  - `Rk_over_R` = `R_k_n / R` — mean/std/min/max (this drives the whole eq)
  - `Gamma_next`, `G_next` — means (financial-friction depth)
  - `Gamma_prime_next`, `G_prime_next` — means (used in ratio_term denom)
  - `omega_bar_next` — mean/std/min/max (Newton output)
  - `ratio_term` — mean
  - `bracket_term` — mean
  - `term_a` = `Rk_over_R * (1 - Gamma_next)` — mean (LHS-style)
  - `term_b` = `ratio_term * bracket_term` — mean (RHS-style)
  - `newton_residual_n_mean` — Newton solver consistency at next state
  - log_residual: tricky here because residual is a difference, not a ratio. Use `term_a - term_b` mean and signed.
**New keys:** `eq8_diag/{Rk_over_R_*, Gamma_next_mean, G_next_mean, Gamma_prime_next_mean, G_prime_next_mean, omega_bar_next_*, ratio_term_mean, bracket_term_mean, term_a_mean, term_b_mean, newton_residual_n_mean, signed_residual_mean, signed_residual_std}`
**Cost:** L (most quantities, financial-friction depth matters most for disaster training)

### 8. eq9 — resource_constraint  [LIGHT]  [STATUS: TODO]
**Residual:** `(g + c + i/mu_ups + Theta*((1-gamma_e)/gamma_e)*(n - w_e) + mu_mon*G_val*R_k*q_lag*k_lag/(mu_z*pi)) / y_z - 1`, an additive identity.
**Decompose into (each as a share-of-y_z):**
  - `g_share` = `g / y_z` — mean
  - `c_share` = `c / y_z` — mean
  - `i_share` = `(i/mu_ups) / y_z` — mean
  - `entrepreneur_share` = `Theta*(1-gamma_e)/gamma_e*(n-w_e) / y_z` — mean
  - `monitoring_share` = `mu_mon*G_val*R_k*q_lag*k_lag / (mu_z*pi*y_z)` — mean
  - `total_share` — mean (should be ≈ 1 + residual)
  - `log_residual` — `log(rhs)` mean/std (here rhs is the expression in parens, which should ≈ y_z)
**New keys:** `eq9_diag/{g_share_mean, c_share_mean, i_share_mean, entrepreneur_share_mean, monitoring_share_mean, total_share_mean, log_residual_mean, log_residual_std}`
**Cost:** S (uses only current defs + states/policies, no rollout)

### 9. eq6 — bond_euler  [SKIP]  [STATUS: TODO]
**Reason:** `(R*beta*lambda_z_n / (pi_n*mu_z_n)) / lambda_z - 1` is a single multiplicative product. Each input is already exposed: `derived/R`, `derived/R_zlb_binding`, `policy/lambda_z`, `policy/pi`, `state/mu_z`. The `eq/eq6_bond_euler` mean already gives the residual. A scalar decomposition would just re-emit means already available. Mark as SKIP with reason.

## Notes
- Helpers that need next-period quantities (eq1, eq3, eq5, eq7, eq8) reuse the eq2/eq4 zero-shock-rollout pattern: `next_states = step_fn(states, policy_out, zeros, c)` then `vmap(policy_fn)(next_states)`.
- Helpers that only need current-period defs (eq2a, eq4a, eq9) can skip the rollout — cheaper.
- Tests live in `tests/test_disaster_diagnostics.py`. Each iteration adds one test function. Test asserts (a) every expected key is present, (b) every value is `float`, (c) every value is finite.
- Surgical commit only — never `git add -A`.
