# Brock-Mirman RBC model configuration
model: brock_mirman
episodes: 2000
batch_size: 64
episode_length: 100
mc_samples: 5

network:
  type: mlp
  hidden_sizes: [64, 64]
  activation: tanh

optimizer:
  name: adam
  learning_rate: 1.0e-3

warm_start: true
log_every: 200
